# 动态操作宣传片

这是“操作界面逻辑宣传片”的动态重制版。画面基于真实产品界面，加入连续推拉镜头、鼠标轨迹、控件高亮、阶段扫描和节奏化转场。

成片不纳入源码仓库或产品 Release；需要时在本地运行下方脚本生成 `自学型熟肉机-动态操作宣传片.mp4`。

## 重新生成

需要 macOS、Node.js、FFmpeg、系统图像转换工具和中文语音 `Tingting`。

```bash
zsh promo/ui-motion/render.sh
```

真实界面截图位于 `assets/raw/`；其余视觉素材和渲染缓存可重新生成，不纳入 Git。
